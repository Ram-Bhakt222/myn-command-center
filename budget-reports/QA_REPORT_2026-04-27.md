# Command Center QA Report — Monday 2026-04-27

**Agent:** weekly-command-center-qa (scheduled task, autonomous run)
**Scope:** 6 Command Center dashboards in `C:\Users\ram\Desktop\Command Center\myn-command-center\`
**Stale-data threshold:** > 7 days
**Today:** 2026-04-27 (Mon)

---

## Headline

**3 ❌ failures, 8 ⚠️ warnings, 6 ✅ passing checks.** Two of the six dashboards named in the task spec no longer exist at the expected paths (consolidated into `index.html` and `agent-control-plane.html`); the task definition itself needs an update. None of the six in-scope dashboards surface the canonical `$38,750/mo MRR / 5 active hotel contracts / pipeline-freeze Day 28` snapshot that lives in `myn.com-main/CLAUDE.md`, so anyone landing on the Command Center today gets an incomplete picture of pipeline state. Ten broken intra-site links across three dashboards still point at archived files. One arithmetic error in hotel-activities (557 + 656 ≠ 1,199; off by 14, which matches the Wave 1 expired-contract count — possibly a stale headline that wasn't recomputed).

---

## ❌ Failures

### F1 — 2 of 6 expected dashboards do not exist at the named path

The task spec asks for `unified-command-center.html` and `agentfield-dashboard.html`. Neither exists at the root anymore:

| Expected file | Status | Replacement (current live master) |
|---|---|---|
| `unified-command-center.html` | Archived → `_archive/deprecated-masters/` | `index.html` ("MYN Command Center v2 — Current vs Future") |
| `agentfield-dashboard.html` | Archived → `_archive/deprecated-agents/` | `agent-control-plane.html` ("MYN Agent Control Plane") |

**Action:** update the QA task SKILL.md to reference the new filenames. Otherwise next Monday's run will keep flagging the same false-positive.

### F2 — 10 broken intra-site links across 3 dashboards

These point at deprecated files that have been archived:

| Source dashboard | Broken target |
|---|---|
| `agent-control-plane.html` | `agent-os-dashboard.html` |
| `agent-control-plane.html` | `agentfield-dashboard.html` |
| `agent-control-plane.html` | `agentic-os-dashboard.html` |
| `agent-control-plane.html` | `dashboard-master.html` |
| `agent-control-plane.html` | `unified-command-center.html` |
| `lead-gen-dashboard.html` | `unified-command-center.html` |
| `team-handoff-dashboard.html` | `agentfield-dashboard.html` |
| `team-handoff-dashboard.html` | `agentic-os-dashboard.html` |
| `team-handoff-dashboard.html` | `dashboard-master.html` |
| `team-handoff-dashboard.html` | `unified-command-center.html` |

**Action:** rewrite the dead anchors to point at `index.html` and `agent-control-plane.html`, or strip them.

### F3 — Canonical pipeline state absent from every in-scope dashboard

Per `myn.com-main/CLAUDE.md` (snapshot 2026-04-23), the load-bearing pipeline numbers are:
`1,199 hotel leads • 5 active contracts • $38,750/mo MRR • pipeline freeze Day 24+ • 28 active scheduled tasks • Budget Agent pause rec. unactioned since Day 11`.

I grepped every in-scope HTML for these and found:

| Number | index | agent-control | analytics | hotel-acts | lead-gen | team-handoff |
|---|---|---|---|---|---|---|
| `$38,750/mo` (MRR) | — | — | — | — | — | — |
| `5 active hotel contracts` | — | — | — | — | — | — |
| `freeze Day N` counter | — | — | — | — | — | — |
| Budget Agent pause rec | — | — | — | — | — | — |
| `28 scheduled tasks` | — | — | — | — | — | — |

Only `master-directory.html` (out of QA scope) carries the `$38,750` figure. The Command Center surfaces *zero* dollars of recurring revenue and *zero* days of pipeline freeze to anyone who opens the live dashboards.

**Action:** add a top-level KPI strip to `index.html` (and mirror to `analytics-dashboard.html`) that pulls from the canonical snapshot in `MYN-Pipeline-Daily.md` or `budget-reports/`.

---

## ⚠️ Warnings

### W1 — Borderline stale: all 6 dashboards stamped `2026-04-21` (6 days old)

Within the 7-day threshold but one day from tipping. Pipeline state has moved since: freeze Day 24 → 28, two new scheduled tasks (`procurement-hub-followup`, `lbsoy-blog-every-3-days`), AUTO_POST_POLICY going live Apr 24, the Apr 22 silent agent-runner day. None of that is reflected.

### W2 — Hotel-activities arithmetic mismatch (557 + 656 = 1,213 ≠ 1,199)

```
Total Hotel Leads: 1,199
Sub: GHL (557) + Email Pipeline (656)
```
557 + 656 = **1,213**, not 1,199. The 14-lead delta exactly matches the 14 expired Wave 1 contracts in CLAUDE.md — strong signal that the headline was set when the lead-count subtracted Wave 1 wins, but the breakdown was updated later without recomputing. Pick one truth.

### W3 — Hotel-activities carries 3 different totals for the same pipeline

`1,199 hotel leads` (KPI strip) / `1,213 implied by breakdown` / `1,395 opportunities` (Stale Pipeline panel, framed as Aug-Sep 2025 GHL CRM data). No reconciling note. Reader can't tell which is the live number.

### W4 — Hotel-activities narrative misframes Wave 1 contracts

> "All 1,395 opportunities are status 'open' from Aug-Sep 2025. **14 contracts were sent but never closed.**"

Per CLAUDE.md these 14 contracts *did* close — they ran for a Wave and then expired. "Sent but never closed" implies they never converted; the actual story is "activated, then ran out, were not renewed." This matters for tone with anyone reading the dashboard cold.

### W5 — `agent-control-plane.html` has no visible "Last updated" stamp

Every other dashboard at least has *something*. This one has nothing — no build date, no data-as-of, no snapshot label. A reader can't tell if they're looking at fresh agent-budget data or week-old.

### W6 — `analytics-dashboard.html` uses misleading client-side timestamp

```js
document.getElementById('lastUpdated').textContent =
  'Last updated: ' + new Date().toLocaleTimeString();
```
This sets "Last updated" to **the user's browser clock at page load**, regardless of when the underlying data was actually built. Anyone glancing at the page will think the dashboard is real-time when in fact the GA4 baseline numbers in the same file are explicitly noted in a JS comment as "30-day averages **as of March 2026**."

### W7 — `analytics-dashboard.html` GA4 baseline ~1 month old

Comment in source: `// Real MYN GA4 baseline numbers (30-day averages as of March 2026)`. Today is end of April 2026; the baseline is ~30+ days stale and is being scaled (`scale = currentDays / 30`) rather than re-pulled.

### W8 — `team-handoff-dashboard.html` has a `<td>TBD</td>` for SYTAR 2025 location

Cosmetic, not data integrity. Cell is in the Conferences table; date and outcome are populated, only the city is missing. Low priority.

---

## ✅ Passing

- **P1.** All 4 of 6 expected dashboards that *do* exist at the right path load with valid HTML and reasonable section structure (`analytics-dashboard.html`, `hotel-activities-dashboard.html`, `lead-gen-dashboard.html`, `team-handoff-dashboard.html`).
- **P2.** No `lorem ipsum`, `TODO:`, `FIXME`, `XXX`, or `[insert ...]` placeholder leakage in 5 of 6 dashboards (only the SYTAR `TBD` in team-handoff, see W8).
- **P3.** Three dashboards have **zero** broken intra-site links: `index.html`, `analytics-dashboard.html`, `hotel-activities-dashboard.html`.
- **P4.** Dashboard byte-size sanity check: all six between 43K and 99K — no obviously truncated/empty pages.
- **P5.** `index.html` (the new unified master) carries an explicit `Updated 2026-04-21` stamp at top — visible freshness signal.
- **P6.** Forward-looking dates in `team-handoff-dashboard.html` (May 12, 2026; Nov 18, 2026) parse as future events, not stale data — confirmed by manual context.

---

## Agent cross-talk protocol — could not run

The skill calls for reading the last 10 messages from `#agent-logs (C0AP2AWRYMV)` via `slack_send_message`/Slack MCP. **No Slack read or write MCP tool is loaded in this scheduled-task environment** — only the `slack-by-salesforce` plugin's *skills* (`slack-search`, `summarize-channel`, etc.), which require an interactive session. I substituted the embedded `myn.com-main/CLAUDE.md` "Current State — 2026-04-23" snapshot as the most recent agent-logs digest available; it is the source for the canonical pipeline numbers in F3.

**Action for Ram:** either wire a Slack MCP into the scheduled-task runner, or rewrite this skill to read `#agent-logs` from a synced file (e.g. `Strategy AGI/agents/agent-logs-snapshot.md`). Otherwise the cross-talk step silently no-ops every Monday.

---

## Slack delivery — also could not auto-post

Same root cause: no Slack write tool. I cannot post the QA report to `#command-center (C0APJF7VA68)` or the activity update to `#agent-logs (C0AP2AWRYMV)` directly. Both messages are written to disk in this folder so Ram can paste them manually:

- This file: `budget-reports/QA_REPORT_2026-04-27.md`
- Slack-ready summary: `budget-reports/QA_REPORT_2026-04-27_SLACK.md`
- Agent-log handoff line: `budget-reports/QA_REPORT_2026-04-27_AGENTLOG.md`

---

## Hands off to

**Ram** — F1 (skill spec update), F2 (broken links), F3 (KPI strip on `index.html`), W1 (refresh trigger), W2 (lead-count reconciliation), and the Slack-MCP plumbing gap. Everything else is cosmetic.
